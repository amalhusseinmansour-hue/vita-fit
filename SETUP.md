# 🏋️ GYM Fitness App - دليل التشغيل الكامل

## 📋 المحتويات
1. [متطلبات النظام](#متطلبات-النظام)
2. [تشغيل الباكاند](#تشغيل-الباكاند)
3. [تشغيل الفرونت اند](#تشغيل-الفرونت-اند)
4. [البنية الكاملة للمشروع](#البنية-الكاملة-للمشروع)
5. [API Endpoints](#api-endpoints)
6. [الأيقونة](#الأيقونة)

---

## 🔧 متطلبات النظام

### الباكاند (Backend)
- Node.js (v16+)
- MongoDB (v5+)
- npm أو yarn

### الفرونت اند (Frontend)
- Flutter SDK (v3.9+)
- Dart SDK
- Android Studio / Xcode (للتشغيل على المحاكي أو الجهاز)

---

## 🚀 تشغيل الباكاند

### 1. تثبيت MongoDB
تأكد من تشغيل MongoDB على المنفذ الافتراضي 27017

### 2. تثبيت الحزم
```bash
cd backend
npm install
```

### 3. إعدادات البيئة
ملف `.env` موجود بالفعل في `backend/.env` بالإعدادات التالية:
```env
PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/gym_db
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRE=7d
MAX_FILE_SIZE=5242880
```

### 4. تشغيل السيرفر
```bash
npm start
# أو للتطوير
npm run dev
```

السيرفر سيعمل على: `http://localhost:5000`

---

## 📱 تشغيل الفرونت اند

### 1. تثبيت الحزم
```bash
flutter pub get
```

### 2. تشغيل التطبيق
```bash
# للأندرويد
flutter run

# لنظام معين
flutter run -d <device-id>
```

### 3. بناء التطبيق
```bash
# للأندرويد
flutter build apk

# للـ iOS
flutter build ios
```

---

## 📂 البنية الكاملة للمشروع

```
gym/
├── backend/                    # الباكاند (Node.js + Express)
│   ├── controllers/           # Controllers
│   │   └── authController.js
│   ├── middleware/            # Middleware
│   │   ├── auth.js
│   │   └── upload.js
│   ├── models/                # MongoDB Models
│   │   ├── User.js
│   │   ├── Workout.js
│   │   ├── Meal.js
│   │   ├── Progress.js
│   │   ├── Subscription.js
│   │   ├── Trainer.js
│   │   └── Workshop.js
│   ├── routes/                # API Routes
│   │   ├── auth.js
│   │   ├── workouts.js
│   │   ├── meals.js
│   │   ├── progress.js
│   │   ├── subscriptions.js
│   │   ├── trainers.js
│   │   ├── users.js
│   │   └── workshops.js
│   ├── uploads/               # ملفات الرفع
│   ├── .env                   # إعدادات البيئة
│   ├── package.json
│   └── server.js              # نقطة البداية
│
├── lib/                       # الفرونت اند (Flutter)
│   ├── constants/             # الثوابت
│   ├── models/                # Data Models
│   ├── providers/             # State Management
│   ├── screens/               # شاشات التطبيق
│   │   ├── splash_screen.dart
│   │   ├── login_screen.dart
│   │   ├── signup_screen.dart
│   │   ├── home_screen.dart
│   │   ├── training_screen.dart
│   │   ├── nutrition_screen.dart
│   │   ├── meal_plans_screen.dart
│   │   ├── progress_tracking_screen.dart
│   │   ├── workout_schedule_screen.dart
│   │   ├── shop_screen.dart
│   │   ├── cart_screen.dart
│   │   ├── profile_screen.dart
│   │   ├── subscription_screen.dart
│   │   └── more_screen.dart
│   ├── services/              # API Services
│   │   └── api_service.dart
│   ├── utils/                 # Utilities
│   ├── widgets/               # Widgets قابلة لإعادة الاستخدام
│   └── main.dart              # نقطة البداية
│
├── assets/                    # الأصول
│   └── icons/                 # الأيقونات
│       ├── icon.png           # أيقونة التطبيق الرئيسية
│       └── app_icon.svg       # أيقونة SVG
│
├── android/                   # مشروع Android
├── ios/                       # مشروع iOS
├── pubspec.yaml               # إعدادات Flutter
├── create_icon.py             # سكريبت إنشاء الأيقونة
├── README.md                  # وصف المشروع
└── SETUP.md                   # هذا الملف
```

---

## 🔌 API Endpoints

### المصادقة (Authentication)
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/me` - معلومات المستخدم الحالي (يتطلب token)
- `PUT /api/auth/updatepassword` - تحديث كلمة المرور (يتطلب token)
- `POST /api/auth/logout` - تسجيل الخروج (يتطلب token)

### التمارين (Workouts)
- `GET /api/workouts` - جلب جميع التمارين (يتطلب token)
- `POST /api/workouts` - إنشاء تمرين جديد (يتطلب token)
- `GET /api/workouts/:id` - جلب تمرين محدد (يتطلب token)
- `PUT /api/workouts/:id` - تحديث تمرين (يتطلب token)
- `DELETE /api/workouts/:id` - حذف تمرين (يتطلب token)

### الوجبات (Meals)
- `GET /api/meals` - جلب جميع الوجبات (يتطلب token)
- `POST /api/meals` - إنشاء وجبة جديدة (يتطلب token)
- `GET /api/meals/daily-nutrition` - التغذية اليومية (يتطلب token)

### الاشتراكات (Subscriptions)
- `POST /api/subscriptions/subscribe` - الاشتراك في خطة (يتطلب token)
- `GET /api/subscriptions/my-subscription` - اشتراكي الحالي (يتطلب token)

### المدربين (Trainers)
- `GET /api/trainers` - جلب جميع المدربين
- `GET /api/trainers/:id` - جلب مدرب محدد

### الورش (Workshops)
- `GET /api/workshops` - جلب جميع الورش
- `POST /api/workshops/:id/register` - التسجيل في ورشة (يتطلب token)
- `GET /api/workshops/user/my-workshops` - ورشي (يتطلب token)

### التقدم (Progress)
- `GET /api/progress` - جلب سجل التقدم (يتطلب token)
- `POST /api/progress` - إضافة تقدم جديد (يتطلب token)
- `GET /api/progress/stats/summary` - إحصائيات التقدم (يتطلب token)
- `GET /api/progress/stats/weight-history` - سجل الوزن (يتطلب token)

---

## 🎨 الأيقونة

تم إنشاء أيقونة التطبيق باستخدام:
- **اللون الأساسي**: `#FF6B35` (برتقالي)
- **التصميم**: دمبل بخلفية متدرجة مع نص "GYM"
- **المقاسات**: تم توليد جميع المقاسات المطلوبة لـ Android و iOS

### إعادة توليد الأيقونة
```bash
# استخدام Python
python create_icon.py

# أو استخدام Flutter
dart run flutter_launcher_icons
```

---

## 🔐 الأمان

### في التطوير
- تم استخدام JWT للمصادقة
- تشفير كلمات المرور باستخدام bcrypt
- Validation على جميع المدخلات

### للإنتاج
1. غيّر `JWT_SECRET` في `backend/.env`
2. استخدم HTTPS
3. حدّث `baseUrl` في `lib/services/api_service.dart`
4. فعّل CORS المناسب

---

## 🐛 استكشاف الأخطاء

### الباكاند لا يعمل
1. تأكد من تشغيل MongoDB
2. تحقق من المنفذ 5000 غير مستخدم
3. راجع ملف `.env`

### الفرونت اند لا يتصل بالباكاند
1. تأكد من تشغيل الباكاند على `http://localhost:5000`
2. تحقق من `baseUrl` في `api_service.dart`
3. تأكد من تعطيل CORS أو إعداده بشكل صحيح

---

## 📝 ملاحظات

- الباكاند يعمل على `http://localhost:5000`
- MongoDB على المنفذ الافتراضي `27017`
- قاعدة البيانات: `gym_db`
- التطبيق يدعم اللغة العربية

---

## 🤝 المساهمة

للمساهمة في المشروع:
1. Fork المشروع
2. إنشاء Branch جديد
3. Commit التغييرات
4. Push إلى الـ Branch
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع خاص ولا يسمح بنشره أو إعادة توزيعه بدون إذن.

---

## 📞 الدعم

للدعم والاستفسارات، يرجى التواصل عبر:
- البريد الإلكتروني: support@gym-app.com

---

**تم بحمد الله ✨**
